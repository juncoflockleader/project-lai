# 场景资产

暂时空着。当前 `build_shot.add_ground()` 直接生成一块 60×60 的纯色地面，
够用了 —— 第一集的戏都在山坡上，地面没有细节。

以后要放的：

| 文件 | 对应 |
|---|---|
| `shan.blend` | 那座山（会裂开的） |
| `huluteng.blend` | 葫芦藤 |
| `wu.blend` | 爷爷的屋 |

约束同 `../characters/README.md`，另外场景资产的原点放在它在世界里该待的位置，
不要每个都归零。
